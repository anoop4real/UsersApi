{
  "name": "users",
  "version": "1.0.0",
  "description": "A sample server to create users, add update delete",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anoop M",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "mongoose": "^5.2.5"
  }
}
